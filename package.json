{
  "name": "abeldewit.github.io",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "nuxt generate && gh-pages -d .output/public"
  },
  "dependencies": {
    "pocketbase": "^0.25.0"
  },
  "devDependencies": {
    "nuxt": "^3.13.0"
  }
}
